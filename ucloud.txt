In classification_pipeline.py

import sys
ROOT = Path(__file__).resolve().parent.parent # folder containing this script
sys.path.insert(0, str(ROOT))

# Own implementations
from utilities.preprocessing import EEG_preprocessing, EMG_preprocessing, RejectBadEpochs, Filtering #E402
from utilities.trainer_and_evaluator import FusionNet_train_eval, SingleNet_train_eval
from utilities.load_and_visualize_data import load_datasets



torch.save({
    "model_name": model_name,
    "model_state": best_state_dict,
    "model_args": model_config, 
    "optimizer_state_dict": best_optimizer_dict,
    "hyperparameters": trial.parameters,}, 
    '{}/model.pth'.format(log_dir))

data_dir = Path(__file__).resolve().parents[1] / 'data/OpenBCI'